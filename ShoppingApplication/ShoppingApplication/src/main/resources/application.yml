server:
  port: 9899
spring:
  application:
    name: ShoppingApplication
  datasource:
    url: jdbc:mysql://localhost:3306/ShoppingApplication
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
      show-sql: true